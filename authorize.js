require("dotenv").config();

const jwt = require("jsonwebtoken");

// here we'll create a function o authenticate the JWT

const authFunction = (req, res, next) => {};

module.exports = authFunction;
